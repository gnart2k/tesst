pipeline {
    agent any

    stages {
        stage('Clone repository') {
            steps {
                // Clone the repository from GitHub
                git 'https://github.com/gnart2k/tesst.git'
            }
        }
    }
}
